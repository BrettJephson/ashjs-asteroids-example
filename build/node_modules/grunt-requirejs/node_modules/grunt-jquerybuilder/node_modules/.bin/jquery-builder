#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../jquery-builder/bin/builder.js" "$@"
  ret=$?
else 
  node  "$basedir/../jquery-builder/bin/builder.js" "$@"
  ret=$?
fi
exit $ret
