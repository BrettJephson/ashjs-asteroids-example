#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jquerybuilder/bin/grunt-jquerybuilder" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jquerybuilder/bin/grunt-jquerybuilder" "$@"
  ret=$?
fi
exit $ret
