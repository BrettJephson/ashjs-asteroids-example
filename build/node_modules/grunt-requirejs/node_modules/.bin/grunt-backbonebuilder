#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-backbonebuilder/bin/grunt-backbonebuilder" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-backbonebuilder/bin/grunt-backbonebuilder" "$@"
  ret=$?
fi
exit $ret
